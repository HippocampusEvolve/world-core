import * as THREE from 'three';
/** Tip at (0,0,0), handle along +Y, scoop opens toward -Z. Height 1.45m. */
export declare function shovel(mats: {
    wood: THREE.Material;
    steel: THREE.Material;
}): THREE.Group;
